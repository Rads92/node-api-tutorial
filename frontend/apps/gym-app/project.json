{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/gym-app",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/gym-app",
        "outputPath": "dist/apps/gym-app"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/gym-app"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "gym-app:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "gym-app:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "gym-app:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "gym-app:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/gym-app"],
      "options": {
        "jestConfig": "apps/gym-app/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/gym-app/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
